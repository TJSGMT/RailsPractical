<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body><br><br>
  <div class="d-flex  align-items-center  justify-content-center mb-3">
    <h1> Product's Index</h1>
  </div>
  <div class="container">
<% if flash[:notice] %> 
  <% flash.each do |name,message| %>
   <p class="text-danger"><%= message%></p>
  <% end %>
<% end %>

<table class="table  table-hover">
  <thead>   
  <tr>
    <th scope="col">Title</th>
    <th scope="col">Description</th>
    <th scope="col">Price</th>
    <th scope="col">Capacity</th>
    <th scope="col">Is active</th>
    <th scope="col">Status</th>
  </tr>
</thead>
<tbody>
  <% @product1s.each do |product1|%>
  <tr> 
    <th scope="row"><%=product1.title%></th>
    <td><%=product1.description%></td>
    <td><%=product1.price%></td>
    <td><%=product1.capacity%></td>
    <td><%=product1.is_active%></td>
    <td><%=product1.status%></td>
    <td><button type="button" class="btn btn-link"><%= link_to "Show", product1_path(product1), class:"btn btn-primary", method: :get %></button>
      <button type="button" class="btn btn-link"><%= link_to " Edit", edit_product1_path(product1), class:"btn btn-primary", method: :get %></button>
      <button type="button" class="btn btn-link"><%= button_to " Destroy", product1_path(product1), class:"btn btn-primary", method: :delete %></button>
    </td>
  </tr>
  <%end%> 
</tbody>
</table><br>                                      
     <button type="button " class="btn btn-link"><%= link_to "Add new product", new_product1_path, class:"btn btn-primary", method: :get %></button> 
</body>
</html>
