<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

<div class="d-flex  align-items-center  justify-content-center">
<h1> Student's Index</h1>
</div><br><br>
<table class="table  table-hover">
  <thead>
    <tr>
    <th scope="col">First name</th>
    <th scope="col">Last name</th>
    <th scope="col">Date of Birth</th>
    <th scope="col">Department</th>
    </tr>
  </thead>
  <tbody>
   <% @students.each do |student|%>
    <tr> 
    <th scope="row"><%=student.firstname%></th>
    <td><%=student.lastname%></td>
    <td><%=student.birthdate%></td>
    <td><%=student.department%></td>
    <td><button type="button" class="btn btn-link"><%= link_to "Show", student_path(student), class:"btn btn-primary", method: :get %></button>
       <button type="button" class="btn btn-link"><%= link_to " Edit", edit_student_path(student), class:"btn btn-primary", method: :get %></button>
       <button type="button" class="btn btn-link"><%= button_to " Destroy", student_path(student), class:"btn btn-primary", method: :delete %></button>
    </td>
    </tr>
    <%end%>
  </tbody>
</table><br>
<button type="button " class="btn btn-link"><%= link_to "Add new student", new_student_path, class:"btn btn-primary", method: :get %></button> 
</body>
</html>
