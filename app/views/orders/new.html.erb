<%if flash[:notice]%>
  <%= flash[:notice]%>
<%end%>
<%= form_for @order do  |f| %>
  <h3><%= f.label :customer_name, class: "form-label" %></h3>
  <p><%= f.select :customer_id,Customer.all.map {|p| [p.fname , p.id ]} %></p><b>
  <h3><%= f.label :product_name,class:"form-label" %></h3>
  <p><%= f.select :product1_id, Product1.all.map {|p| [p.title , p.id ]} %></p><br>
  <h4><%= f.label :Quantity, class: "form-label" %></h4>
  <h4><%= f.text_field :quantity, class: "form-control" %></h4> <br>
  <h4><%= f.label :status, class: "form-label" %></h4>
  <p><%= f.select :status, Order.statuses.keys,class: "form-control" %></p>
  <h4><%= f.submit class: "btn btn-primary"  %></h4>
<% end %>
